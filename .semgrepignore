# Documentation — contains placeholder/example secrets, not real credentials.
# Trivy handles secret scanning of actual source files separately.
docs/

# pnpm-workspace.yaml: trustPolicy intentionally omitted — next-auth@5.0.0-beta.31
# lacks provenance attestation, making no-downgrade incompatible until v5 stable ships.
# Enforced in CI instead (security.yml trust-policy job). Re-enable here when next-auth
# v5 stable ships with provenance.
pnpm-workspace.yaml

# Generated / vendored
node_modules/
.pnpm-store/
.next/

# Test fixtures — may contain intentionally insecure patterns for testing
tests/
e2e/
